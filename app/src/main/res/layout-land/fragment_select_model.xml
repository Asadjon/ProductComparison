<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="com.cyberpantera.productcomparison.R"/>
        <variable
            name="viewModel"
            type="com.cyberpantera.productcomparison.fragments.select_model.SelectModelViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        tools:context=".fragments.select_model.SelectModelFragment">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:layout_gravity="center_vertical"
            android:layout_weight=".5">

            <com.mikhaellopez.circularimageview.CircularImageView
                style="@style/select_model.image"
                android:src="@{viewModel.productLiveData.src}"/>

            <TextView
                style="@style/select_model.product_name"
                android:text="@{viewModel.productLiveData.name}"
                android:layout_weight="0.25"/>

        </LinearLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingHorizontal="20dp"
            android:layout_gravity="center_vertical"
            android:layout_weight="1">

            <NumberPicker
                app:maxValue="@{viewModel.maxValueOfNumPicker}"
                app:displayedValues="@{viewModel.displayedValuesOfNumPicker}"
                app:value="@{viewModel.getValueOfNumPicker(0)}"
                app:valueChanged="@{viewModel.getOnValueChangeListenerOfNumPicker(0)}"
                style="@style/select_model.number_picker"/>

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                app:enabled="@{viewModel.compareButtonEnabledLiveData}"
                android:src="@drawable/baseline_compare_arrows_24"
                android:onClick="@{() -> viewModel.onClickCompare()}"
                style="@style/select_model.compare_button" />

            <NumberPicker
                app:maxValue="@{viewModel.maxValueOfNumPicker}"
                app:displayedValues="@{viewModel.displayedValuesOfNumPicker}"
                app:value="@{viewModel.getValueOfNumPicker(1)}"
                app:valueChanged="@{viewModel.getOnValueChangeListenerOfNumPicker(1)}"
                style="@style/select_model.number_picker"/>

        </LinearLayout>

    </LinearLayout>
</layout>