<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="com.cyberpantera.productcomparison.R"/>
        <variable
            name="viewModel"
            type="com.cyberpantera.productcomparison.fragments.select_model.SelectModelViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".fragments.select_model.SelectModelFragment">

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/image_view_bottom"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:orientation="horizontal"
            app:layout_constraintGuide_percent="0.4"/>

        <com.mikhaellopez.circularimageview.CircularImageView
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:src="@{viewModel.productLiveData.src}"
            app:civ_border_color="@color/teal_200"
            app:civ_border_width="4dp"
            app:civ_shadow="true"
            app:civ_shadow_color="@color/teal_700"
            app:civ_shadow_radius="10dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/image_view_bottom"/>

        <TextView
            android:id="@+id/text_view"
            style="@style/product_item_text_style"
            app:layout_constraintTop_toBottomOf="@id/image_view_bottom"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            android:text="@{viewModel.productLiveData.name}"
            android:textSize="25sp"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="70dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/text_view"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintWidth_percent=".95">

            <NumberPicker
                android:id="@+id/model_picker_1"
                android:onValueChange="@{() -> viewModel.onValueChange()}"
                style="@style/select_model_number_picker"
                android:theme="@style/select_model_number_picker" />

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginHorizontal="20dp"
                android:background="@android:color/transparent"
                android:backgroundTint="@color/teal_200"
                android:contentDescription="Compare"
                android:src="@drawable/baseline_compare_arrows_24"
                android:onClick="@{() -> viewModel.onClickCompare()}"
                android:enabled="@{viewModel.compareButtonEnabledLiveData}"/>

            <NumberPicker
                android:id="@+id/model_picker_2"
                android:onValueChange="@{() -> viewModel.onValueChange()}"
                style="@style/select_model_number_picker"
                android:theme="@style/select_model_number_picker"/>

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>